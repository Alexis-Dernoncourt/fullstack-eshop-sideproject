import { Container } from './Announcement.style';

const Announcement = () => {
    return (
        <Container>
            Les frais de port sont offerts + Profitez de jusqu'à -30% sur tout
            le site !
        </Container>
    );
};

export default Announcement;
